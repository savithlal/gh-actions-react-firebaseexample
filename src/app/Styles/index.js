import { StyleSheet } from 'react-native';

/**
 * GlobalStyles: Contains styles that are commonly used throughout the app
 */
const GlobalStyles = StyleSheet.create({
    mainView: {
        flex: 1,
    },
})

export default GlobalStyles